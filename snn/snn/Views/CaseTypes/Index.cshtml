@model IEnumerable<snn.Models.CaseType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CaseTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CaseTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CaseTypeID })
        </td>
    </tr>
}

</table>
