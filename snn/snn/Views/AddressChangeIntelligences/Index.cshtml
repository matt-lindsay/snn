@model IEnumerable<snn.Models.AddressChangeIntelligence>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AddressChangeIntelligenceDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AddressChangeIntelligenceDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AddressChangeIntelligenceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AddressChangeIntelligenceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AddressChangeIntelligenceID })
        </td>
    </tr>
}

</table>
