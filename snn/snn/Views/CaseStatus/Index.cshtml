@model IEnumerable<snn.Models.CaseStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StatusDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StatusDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CaseStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CaseStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CaseStatusID })
        </td>
    </tr>
}

</table>
